import React from "react";
import "./clock.css";

const clock = () => {
  return (
    <div className="clock-wrap">
      <div className="numbers">12 : 50</div>
    </div>
  );
};

export default clock;
